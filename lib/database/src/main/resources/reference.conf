postgres {
  host: "localhost"
  host: ${?PG_HOST}
  port: 5432
  port: ${?PG_PORT}
  database: ${?PG_DATABASE}
  schema: public
  schema: ${?PG_SCHEMA}
  username: ${?PG_USER}
  password: ${?PG_PASSWORD}
  maxPoolSize: 10
  maxPoolSize: ${?PG_P00L_SIZE}
  autoTransaction: false
  autoTransaction: ${?PG_AUTO_TRANSACTION}
  ipType: "PRIVATE"
  ipType: ${?PG_IP_TYPE}
}